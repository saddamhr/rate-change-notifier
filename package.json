{
  "name": "rate-increase-notifier",
  "version": "1.0.0",
  "description": "A Node.js app to notify rate changes",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "test": "jest --coverage",
    "dev": "nodemon src/index.js",
    "build": "babel src -d dist",
    "lint": "eslint src",
    "prebuild": "npm run lint && npm test",
    "clean": "rm -rf dist && mkdir dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.7.7",
    "dotenv": "^16.4.5",
    "node-cron": "^3.0.3",
    "nodemailer": "^6.9.16"
  },
  "devDependencies": {
    "@babel/preset-env": "^7.26.0",
    "babel-cli": "^6.26.0",
    "eslint": "^9.14.0",
    "jest": "^29.7.0",
    "nodemon": "^3.1.7"
  }
}
